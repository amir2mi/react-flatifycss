import { Canvas, Story } from '@storybook/addon-docs';

# Box

<p class="component-doc-intro">
  Element is a general purpose component to extend with the power of FlatifyCSS
  features and styled-components.
</p>
<div class="funky-spacer" />

## Common example

Let's create a card with `</Element>` component, using `sx` prop to apply styles with styled-components library and `as` prop to define the semantic for HTML tag.

<Canvas>
  <Story id="components-element--default" />
</Canvas>
